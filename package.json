{
  "name": "attendance-portal",
  "version": "1.0.0",
  "description": "A comprehensive attendance management system with user authentication, leave management, and holiday tracking",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["attendance", "portal", "management", "react", "nodejs", "mongodb"],
  "author": "Attendance Portal Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 